fastapi>=0.110.0
# starlette 1.0 changed the TemplateResponse signature (request now first
# positional arg). Pin to the new-API range so older API regressions or
# unexpected 2.0 jumps don't slip in via fastapi's loose dep range.
starlette>=1.0,<2.0
uvicorn>=0.29.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
httpx>=0.27.0
python-multipart>=0.0.9
pyyaml>=6.0.1
jinja2>=3.1.3
markdown>=3.6
bleach>=6.0
